@model IEnumerable<FutsalSemuaSenang.Models.Booking>

@{ Layout = "_AdminLayout"; }

<br />
<h3 align="center" style="font-family:'Comic Sans MS'">Semua Order Booking Belum Terkonfirmasi</h3>
<br />

<div class="card">
    <table class="table">
        <thead>
            <tr>
                <td align="center" width="5%">
                    <strong style="font-size:15px">Nomor</strong>
                </td>
                <td align="center" width="5%">
                    <strong style="font-size:15px">Id User</strong>
                </td>
                <td align="center" width="10%">
                    <strong style="font-size:15px">Nama Lapangan</strong>
                </td>
                <td align="center" width="10%">
                    <strong style="font-size:15px">Tanggal Booking</strong>
                </td>
                <td align="center" width="10%">
                    <strong style="font-size:15px">Jam Mulai</strong>
                </td>
                <td align="center" width="10%">
                    <strong style="font-size:15px">Jam Selesai</strong>
                </td>
                <td align="center" width="10%">
                    <strong style="font-size:15px">Harga</strong>
                </td>
                <td align="center" width="10%"><strong style="font-size:15px">Konfirmasi Booking</strong></td>
            </tr>
        </thead>
        <tbody>
            @{
                var nomor = 1;
            }
            @foreach (var item in Model)
            {
                <tr>
                    <td align="center">
                        @nomor
                        @{
                            nomor += 1;
                        }
                    </td>
                    <td align="center">
                        @Html.DisplayFor(modelItem => item.IdUser)
                    </td>
                    <td align="center">
                        @Html.DisplayFor(modelItem => item.NamaLapangan)
                    </td>
                    <td align="center">
                        @Html.DisplayFor(modelItem => item.Tanggal)
                    </td>
                    <td align="center">
                        @Html.DisplayFor(modelItem => item.JamMulai)
                    </td>
                    <td align="center">
                        @Html.DisplayFor(modelItem => item.JamSelesai)
                    </td>
                    <td align="center">
                        @Html.DisplayFor(modelItem => item.Harga)
                    </td>
                    <td align="center">
                        <a href="@Url.Action("Konfirmasi", "Home", new { id = item.Id })"
                           class="fas fa-upload fa-2x" title="Konfirmasi" style="color:green"
                           onclick="if (! confirm('Yakin untuk Mengkonfirmasi Order Booking ini ?')) { return false; }">
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
<br />
<br />